# Basic Express NodeJS Server

Simple Express server that demonstrates:
- Basic routes (/ and /about)
- Conditional routing with next() (/foo)
- Regex route (/user and /username)
- Dynamic route param (/user/:username)
- Query string handling (/get)
- 404 handler

## Setup
```bash
npm install
